replicaCount: 1

image:
  repository: traveltrack-api
  tag: v1.0
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "250m"
    memory: "256Mi"

config:
  appVersion: "1.0.0"
